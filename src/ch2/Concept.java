package ch2;

/**
 * 通过行为参数化传递代码
 *
 * 有点像mybatis动态sql
 *
 * 行为参数化，包括了  类，匿名类，lambda的参数化
 * 第一章的：是方法的参数化。方法可以作为参数
 */
public class Concept {

    /**
     * 以下是你应从本章中学到的关键概念。
     *  行为参数化，就是一个方法接受多个不同的行为作为参数，并在内部使用它们，完同行为的能力。
     *  行为参数化可让代码更好地适应不断变化的要求，减轻未来的工作量。
     *  传递代码，就是将新行为作为参数传递给方法。但在Java 8之前这实现起来很啰嗦。
     * 口声明许多只用一次的实体类而造成的啰嗦代码，
     * 在Java 8之前可以用匿名类来减少 Java API包含很多可以用不同行为进行参数化的方法包括排序、线程和GUI处理。
     */
}

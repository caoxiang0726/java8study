package jdk8inaction.ch1.sec2.title1;

/**
 *  1.2.1 方法和 Lambda 作为一等公民
 */
public class Concept {
    /**
     * 编程语言的整个目的就在于操作值，要是按照历史上编程语言的传统，这些值因此被称为一等值
     * 编程语言中的其他结构也许有助于我们表示值的结构，但在程序执行期间
     * 不能传递，因而是二等公民。前面所说的值是Java中的一等公民，但其他很多Java概念（如方法
     * 和类等）则是二等公民。
     *
     * 人们发现，在运行时传递方法能将方法变成一等公民。
     */
}
